name: Bundlesize
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  bundlesize:
    name: Check Bundle Size
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: 'old'
          ref: 'master'
      - uses: actions/checkout@v2
        with:
          path: 'new'

      - name: Check Bundle Size Change
        uses: "BinaryMuse/action-bundlesize@4066b0c"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
